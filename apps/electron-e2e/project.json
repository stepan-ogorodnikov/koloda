{
  "name": "electron-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/electron-e2e/src",
  "projectType": "application",
  "tags": ["type:e2e", "platform:desktop"],
  "implicitDependencies": ["@koloda/electron", "@koloda/electron-react"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsc --project apps/electron-e2e/tsconfig.json --noEmit"
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "dependsOn": [
        {
          "target": "build-rust",
          "projects": ["@koloda/electron"]
        }
      ],
      "options": {
        "commands": [
          "bunx swc apps/electron/src/preload.ts -o apps/electron/dist/preload.js -C module.type=commonjs",
          "bunx playwright test --config apps/electron-e2e/playwright.config.ts"
        ],
        "parallel": false
      }
    }
  }
}
