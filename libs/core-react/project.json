{
  "name": "@koloda/core-react",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "tags": ["type:lib"],
  "sourceRoot": "libs/core-react/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx vitest run --config libs/core-react/vitest.config.mjs --configLoader runner"
      },
      "inputs": ["default", "^default"]
    }
  }
}
