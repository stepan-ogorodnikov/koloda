{
  "name": "react",
  "sourceRoot": "libs/react/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx vitest run --config libs/react/vitest.config.mjs --configLoader runner"
      }
    }
  }
}
